import {useRef} from 'react';
export default function DropZone({onFile}){const ref=useRef();const pick=e=>e.target.files?.[0]&&onFile(e.target.files[0]);return <main className="drop"><section className="drop-card" onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();const f=e.dataTransfer.files[0];if(f)onFile(f)}}><div className="eyebrow">Privacy-first chat analytics</div><h1>Chat Wrapped</h1><p>Drop a WhatsApp text export here. Everything is processed locally in your browser.</p><label className="file-label">Choose chat export<input ref={ref} type="file" accept=".txt,text/plain" onChange={pick}/></label></section></main>}
