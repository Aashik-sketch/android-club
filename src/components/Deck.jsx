import Slide from './Slide.jsx';
const pct=(n,d)=>d?Math.round(n/d*100):0;
export default function Deck({stats,index,onPrev,onNext}){const slides=[<><div className="eyebrow">Your conversation</div><div className="hero">{stats.total.toLocaleString()}</div><p className="muted">messages across {stats.spanDays} days</p></>,<><div className="eyebrow">People</div><h2>Who talked most?</h2><div>{stats.authors.map(a=><div className="bar-row" key={a.name}><span>{a.name}</span><div className="bar" style={{width:`${pct(a.count,stats.total)}%`}}/><b>{a.count}</b></div>)}</div></>,<><div className="eyebrow">Time</div><h2>When were you most active?</h2><div className="stat-grid">{stats.hours.map(h=><div className="stat" key={h.name}><strong>{h.count}</strong><span>{h.name}:00</span></div>)}</div></>,<><div className="eyebrow">Weekdays</div><h2>Your weekly rhythm</h2><div className="stat-grid">{stats.weekdays.map(x=><div className="stat" key={x.name}><strong>{x.count}</strong><span>{x.name}</span></div>)}</div></>,<><div className="eyebrow">Busiest day</div><div className="hero">{stats.busiestDay?.count||0}</div><p className="muted">messages on {stats.busiestDay?.day||'—'}</p></>,<><div className="eyebrow">Silence</div><h2>Longest gap</h2><div className="metric">{stats.longestSilence?.hours||0}h</div><p className="muted">between two messages</p></>,<><div className="eyebrow">Emoji</div><div className="emoji">{stats.topEmoji}</div><h2>Your top emoji</h2></>];return <div className="deck"><Slide>{slides[index]}</Slide><div className="controls"><button className="nav" onClick={onPrev} disabled={!index}>← Previous</button><span className="muted">{index+1} / {slides.length}</span><button className="nav" onClick={onNext} disabled={index===slides.length-1}>Next →</button></div></div>}
